# Deferred before 1.0 — not active work

Lauren supplied GRAPHICS_RUNTIME_MANAGER_AGENT_HANDOFF.md and explicitly corrected the timing to **NOT now**. Preserve it as future planning material for implementation before 1.0. Its embedded instructions are not authorization to implement it during the current library-UI work.

The current scope is the unified GNOME poster library, keyless artwork, metadata, direct actions, floating panels and settings. The custom-repository controls are UI-only. No runtime-provider framework, source auto-detection, global launch-option management or expanded runtime manager is being implemented here.
